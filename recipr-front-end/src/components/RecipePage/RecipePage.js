import React from 'react'
import { MyContext } from "../../context";

function RecipePage({ strMeal, strMealThumb, idMeal, strInstructions, strIngredient1 }) {
    return (
        <div>
            <h1>{Meals}</h1>
        </div>
    )
}

export default RecipePage
